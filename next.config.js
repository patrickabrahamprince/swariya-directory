/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['images.unsplash.com', 'via.placeholder.com', 'picsum.photos', 'fastly.picsum.photos', 'loremflickr.com'],
  },
}
module.exports = nextConfig
